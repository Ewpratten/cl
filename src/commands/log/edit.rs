pub fn exec_edit_log(
    id: &str,
    callsign: &str,
    frequency: f32,
    mode: &str,
    time: Option<&str>,
    grid: Option<&str>,
    name: Option<&str>,
    notes: Option<&str>,
) {
    todo!();
}
